# OSV-Scanner ignore list — frontend/package-lock.json.
# GHSA-qwww-vcr4-c8h2 affects react-router's RSC / server-action mode only.
# This frontend is a Vite-built SPA using declarative client routing only —
# no RSC, no server actions, no server rendering. Fix requires the
# react-router 8.x major bump; scheduled with the next frontend dependency
# cycle (dependabot tracks it).
[[IgnoredVulns]]
id = "GHSA-qwww-vcr4-c8h2"
reason = "RSC/server-action advisory; SPA uses client-side routing only. Major bump to 8.x scheduled via dependabot."
ignoreUntil = 2026-11-01
