[project]
name = "emerge-tools"
version = "0.1.0"
description = "EmergeOS platform system MCP tool manifests and vendored servers"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0",
]

[tool.uv]
package = false
