[project]
name = "common-utils"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "bcrypt>=4.1.0",
    "pydantic>=2.5.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
