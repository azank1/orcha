[project]
name = "common-database"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "prisma>=0.11.0",
    "asyncpg>=0.29.0",
    "tomlkit>=0.12.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
