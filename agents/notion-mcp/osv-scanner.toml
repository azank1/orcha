# OSV-Scanner ignore list — agents/notion-mcp/package-lock.json.
# All findings are in the vitest 2 devDependency chain (esbuild / vite /
# vitest). They affect the dev/test server only and are never shipped in the
# built agent. Closing them requires the vitest 4 major upgrade, pending
# API-compat review.
[[IgnoredVulns]]
id = "GHSA-67mh-4wv8-2f99"
reason = "Dev-only (esbuild in vitest 2 chain); not shipped. vitest 4 major bump pending."

[[IgnoredVulns]]
id = "GHSA-4w7w-66w2-5vf9"
reason = "Dev-only (vite 5 in vitest 2 chain); not shipped. vitest 4 major bump pending."

[[IgnoredVulns]]
id = "GHSA-fx2h-pf6j-xcff"
reason = "Dev-only (vite 5 in vitest 2 chain); not shipped. vitest 4 major bump pending."

[[IgnoredVulns]]
id = "GHSA-v6wh-96g9-6wx3"
reason = "Dev-only (vite 5 in vitest 2 chain); not shipped. vitest 4 major bump pending."

[[IgnoredVulns]]
id = "GHSA-5xrq-8626-4rwp"
reason = "Dev-only (vitest 2.1.9); not shipped. vitest 4 major bump pending."
